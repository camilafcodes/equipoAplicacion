# equipoAplicacion

## pegar esto en el agente tech-lead

Use issue #1 as the source of truth. Follow your default workflow:
1) If monorepo scaffold is missing, bootstrap it per .github/copilot-instructions.md
2) Generate /docs/architecture.md, /docs/api-contract.md, /docs/tasks.md from issue #1
3) Open a PR with all changes and include run instructions in README.